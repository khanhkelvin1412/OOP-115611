package Tuan6;

public class Heritance {
    // gói java.lang được tự động import trong mọi class
    // trong gói java.lang có 3 phương thức chủ yếu:
    // + toString(): chuyển đối tượng -> xâu
    // + equals(): so sánh đối tượng
    // nguyên lí truy cập:
//    + public : ở đâu cũng truy cập được
 //   + package: trong cùng gói
    // + private: chỉ được cùng lớp
    // + protected: cùng gói hoặc lớp con. riêng trường hợp protected : ko truy cập được các package khác gói nhưng lại
    // kế thừa được
    // khi kế thừa thì lớp con cùng gói chỉ truy cập được public, protected, default
    // khi kế thừa khác gói thì lớp con chỉ kế thừa được public và protected
//
//
//
//
//


//chỉ định truy cập default: dùng trong khác lớp, cùng gói
}
